application.args=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/eLife.jar
dist.javadoc.dir=${dist.dir}/javadoc
file.reference.jython.jar=ext/jython.jar
file.reference.RXTXcomm.jar=ext/RXTXcomm.jar
jar.compress=false
javac.classpath=\
    ${libs.Jetty6b12.classpath}:\
    ${libs.Jython.classpath}:\
    ${libs.Quartz1.5.2.classpath}:\
    ${libs.JDom1.0.classpath}:\
    ${libs.JRobin1.4.0.classpath}:\
    ${libs.RXTX-2.1-7.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.4
javac.target=1.4
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.xmlunit.classpath}:\
    ${libs.Junit-addons1.4.1.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.Jython.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.Jython.classpath=../../../../../Java/jython-2.1/1.5 Compabable/jython.jar
main.class=au.com.BI.Home.eLife
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=${file.reference.server-src}
test.src.dir=${file.reference.server-test}
application.args=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/eLife.jar
dist.javadoc.dir=${dist.dir}/javadoc
jar.compress=false
javac.classpath=\
    ${libs.Jetty6b12.classpath}:\
    ${libs.Jython.classpath}:\
    ${libs.Quartz1.5.2.classpath}:\
    ${libs.JDom1.0.classpath}:\
    ${libs.JRobin1.4.0.classpath}:\
    ${libs.RXTX-2.1-7.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.xmlunit.classpath}:\
    ${libs.Junit-addons1.4.1.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.JDom.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JDom.classpath=../../../../../Java/jdom-1.0/build/jdom.jar
# Property libs.Jetty6b12.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.Jetty6b12.classpath=../../../../../Java/jetty-6.0.0beta12/lib/jetty.jar:../../../../../Java/jetty-6.0.0beta12/lib/servlet-api-2.5.jar
# Property libs.JRobin.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JRobin.classpath=../../../../../Java/jrobin-1.4.0/lib/jrobin-1.4.0.jar:../../../../../Java/jrobin-1.4.0/lib/snmp-1.3.jar
# Property libs.Jython.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.Jython.classpath=../../../../../Java/jython-2.1/1.5 Compabable/jython.jar
# Property libs.Quartz1.5.2.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.Quartz1.5.2.classpath=../../../../../Java/quartz-1.5.2/quartz-1.5.2.jar:../../../../../Java/quartz-1.5.2/lib/core/commons-collections-3.1.jar:../../../../../Java/quartz-1.5.2/lib/core/commons-logging-api.jar:../../../../../Java/quartz-1.5.2/lib/core/commons-logging.jar
main.class=au.com.BI.Home.eLife
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=Java_HotSpot_TM__Client_VM_1.4.2-54
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=${file.reference.server-src}
test.src.dir=${file.reference.server-test}
