application.args=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/eLife.jar
dist.javadoc.dir=${dist.dir}/javadoc
file.reference.eLife-src=src
file.reference.eLife-test=test
jar.compress=false
javac.classpath=\
    ${libs.Jython2.1.classpath}:\
    ${libs.Quartz1.5.2.classpath}:\
    ${libs.RXTX2.1-7.classpath}:\
    ${libs.JDom1.0.classpath}:\
    ${libs.JRobin.classpath}:\
    ${libs.Groovy1.0.classpath}:\
    ${libs.Jetty6.1.0.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.Junit-addons-1.4.classpath}:\
    ${libs.xmlunit-1.0.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.Groovy1.0.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.Groovy1.0.classpath=../../../../../Java/groovy-1.0/embeddable/groovy-all-1.0.jar
# Property libs.Jetty6.1.0.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.Jetty6.1.0.classpath=../../../../../Java/jetty-6.1.0/lib/jetty-6.1.0.jar:../../../../../Java/jetty-6.1.0/lib/jetty-util-6.1.0.jar:../../../../../Java/jetty-6.1.0/lib/servlet-api-2.5.jar
main.class=au.com.BI.Home.eLife
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=${file.reference.eLife-src}
test.src.dir=${file.reference.eLife-test}
